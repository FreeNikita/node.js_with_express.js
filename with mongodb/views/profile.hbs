<h1>Профиль</h1>

<div class="row">
    <div class="col s12 m12 l6">
        {{#if user.avatarUrl }}
            <img
                class="avatar"
                src="/{{user.avatarUrl}}"
                alt=""
            >
        {{else }}
            <p>Avatar is empty</p>
        {{/if}}

    </div>

    <div class="col s12 m12 l6">
        <form action="/profile" method="POST" enctype="multipart/form-data">
            <p>Your email: <strong>{{user.email}}</strong></p>

            <div class="input-field">
                <input id="name" name="name" type="text" class="validate" required value="{{user.name}}">
                <label for="name">Your name</label>
                <span class="helper-text" data-error="Enter your name"></span>
            </div>

            <div class="file-field input-field">
                <div class="btn">
                    <span>Avatar</span>
                    <input type="file" name="avatar">
                </div>
                <div class="file-path-wrapper">
                    <input class="file-path validate" type="text">
                </div>
            </div>

            <input type="hidden" name="_csrf" value="{{csrf}}">

            <button type="submit" class="btn">Update</button>
        </form>

        <p class="info">This is a test project, that uses free Heroku service, because of that you could update the image.
            The image is able to update but after the refresh, the image disappears.</p>
    </div>
</div>